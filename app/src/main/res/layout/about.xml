<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:id="@+id/root"
    android:padding="10dip">

    <TextView android:id="@+id/about_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/app_name"
                android:gravity="center_horizontal"
                android:layout_centerHorizontal="true"
                android:textSize="40px"
                android:textStyle="bold"/>

    <TextView android:id="@+id/about_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="est un application contient la description des gouvernorats de la Tunisie développé par Master Mpdam"
        android:gravity="center_horizontal"
        android:textSize="30px"
        android:layout_margin="10dip"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/about_title"/>
    <ImageView
        android:id="@+id/about_logo"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_gravity="center"
        android:src="@drawable/logo_team"
        android:layout_below="@+id/about_desc"
        android:layout_centerHorizontal="true" />

</RelativeLayout>